// Smoke test placeholder.
// The app requires Firebase.initializeApp() before it can render.
// Full end-to-end tests live in integration_test/app_test.dart
// and use the Firebase Local Emulator Suite.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — Firebase required, use integration_test/ instead', () {
    expect(true, isTrue);
  });
}
